local RS = game:GetService("ReplicatedStorage")

local e = Instance.new("RemoteEvent")
e.Name = "KickPlayer"
e.Parent = RS

e.OnServerEvent:Connect(function(plr, target)
	if target and target:IsA("Player") then
		target:Kick("Bạn đã bị kick!")
	end
end)
